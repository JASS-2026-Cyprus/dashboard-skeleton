# Cockpit skeleton

To be used as context for per-team dashboard design.

Each team should contribute a widget to the `Overview` page summarising the findings of their agent as well as a page in their corresponding page. See the `Maintenace` tab design for an example.
